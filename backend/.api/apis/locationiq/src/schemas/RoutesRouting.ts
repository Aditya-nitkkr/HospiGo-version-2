import RouteRouting from './RouteRouting.js';

const RoutesRouting = {
  "title": "routes-routing",
  "type": "array",
  "description": "Represents a route through (potentially multiple) waypoints.",
  "items": RouteRouting,
  "x-readme-ref-name": "routes-routing"
} as const;
export default RoutesRouting
